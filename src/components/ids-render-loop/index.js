// Use for global renderloop access
export { default as renderLoop } from './ids-render-loop-global';

// Use for constructing a new renderloop
export {
  IdsRenderLoop,
  IdsRenderLoopItem
} from './ids-render-loop';
