// Ids Editor
import IdsEditor from '../ids-editor';

// Initialize the 4.x
document.addEventListener('DOMContentLoaded', () => {
  console.log($);
  $('body').initialize();
  $('#editor-4x').editor();
});
