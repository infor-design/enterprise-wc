export class IdsInverseColorMixin {
  /** true if the "inverse" style is currently applied */
  inverse: boolean;

  /** refreshes the "inverse" state applied to the IdsElement container */
  refreshInverse(): void;
}
