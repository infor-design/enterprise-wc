import './example.scss';

document.addEventListener('DOMContentLoaded', () => {
  // Display the message once the page loads
  const message = document.querySelector('ids-message');
  message.show();
});
