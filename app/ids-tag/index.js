import IdsTag from '../../src/ids-tag/ids-tag';

// Add an event listener to test clickable links
document.querySelector('#ids-clickable-tag').addEventListener('click', (e) => {
  console.log('Click Fired', e); //eslint-disable-line
});
